---
# i3 + dmenu installed via ui_packages; config stowed by the dotfiles i3 package.
# Placeholder for future i3-only setup (e.g. picom, feh).
- name: I3 config is managed by dotfiles
  ansible.builtin.debug:
    msg: i3 config arrives via the dotfiles i3 package (stow)
